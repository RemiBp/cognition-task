/** Identifiers of the walkthrough case, shared by the seed, the journey script and tests. */
export const DEMO_CASE_ID = "demo-kyc-mateo";
export const DEMO_CASE_REFERENCE = "KYC-DEMO-001";
export const DEMO_CUSTOMER = "Mateo Moreau";

/**
 * Deliberately unresolved: the file shows why a reviewer might hesitate, and
 * the resolution only exists once a reviewer writes it down.
 */
export const DEMO_EVIDENCE =
  "Synthetic demo file. Declared identity matches the onboarding form and sanctions and PEP screening returned no hit. One adverse media article from 2019 names a Mateo Moreau in a fraud investigation; the article gives no date of birth, and the declared nationality differs from the one it mentions. No identity document is stored and nothing here was verified automatically.";
